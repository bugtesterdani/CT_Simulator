<Window x:Class="Ct3xxSimulator.Desktop.Views.LiveStateWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Live-Zustand"
        Width="980"
        Height="720"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="240"/>
        </Grid.RowDefinitions>

        <Border Padding="12" Background="#FFF6F8FA" BorderBrush="#FFD7DEE7" BorderThickness="1" CornerRadius="8">
            <StackPanel>
                <TextBlock x:Name="CurrentStepTextBlock" FontSize="20" FontWeight="Bold" Text="Aktueller Schritt: -"/>
                <TextBlock x:Name="CurrentTimeTextBlock" Margin="0,6,0,0" Foreground="#FF5A6470" Text="Simulationszeit: 0 ms"/>
            </StackPanel>
        </Border>

        <Grid Grid.Row="1" Margin="0,12,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.6*"/>
                <ColumnDefinition Width="1.1*"/>
                <ColumnDefinition Width="1.1*"/>
                <ColumnDefinition Width="1.1*"/>
                <ColumnDefinition Width="1.2*"/>
                <ColumnDefinition Width="1.2*"/>
            </Grid.ColumnDefinitions>

            <GroupBox Header="Signale" Margin="0,0,12,0">
                <DataGrid x:Name="SignalsGrid" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Signal" Binding="{Binding Name}" Width="*"/>
                        <DataGridTextColumn Header="Wert" Binding="{Binding Value}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </GroupBox>

            <GroupBox Grid.Column="1" Header="Messbusse" Margin="0,0,12,0">
                <DataGrid x:Name="MeasurementBusGrid" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Bus" Binding="{Binding Name}" Width="*"/>
                        <DataGridTextColumn Header="Signal" Binding="{Binding Value}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </GroupBox>

            <GroupBox Grid.Column="2" Header="DUT Inputs" Margin="0,0,12,0">
                <DataGrid x:Name="DeviceInputsGrid" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                        <DataGridTextColumn Header="Wert" Binding="{Binding Value}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </GroupBox>

            <GroupBox Grid.Column="3" Header="DUT Outputs" Margin="0,0,12,0">
                <DataGrid x:Name="DeviceOutputsGrid" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                        <DataGridTextColumn Header="Wert" Binding="{Binding Value}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </GroupBox>

            <GroupBox Grid.Column="4" Header="Relais / Elemente" Margin="0,0,12,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <ListBox x:Name="RelayStateList"/>
                    <ListBox x:Name="ElementStateList" Grid.Row="1"/>
                </Grid>
            </GroupBox>

            <GroupBox Grid.Column="5" Header="Faults / DUT Internals">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <ListBox x:Name="FaultList"/>
                    <DataGrid x:Name="DeviceInternalsGrid" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                            <DataGridTextColumn Header="Wert" Binding="{Binding Value}" Width="*"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </GroupBox>
        </Grid>

        <GroupBox Grid.Row="2" Header="Signalverlauf" Margin="0,12,0,0">
            <Grid Margin="8">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <ComboBox x:Name="SignalHistorySelector" Width="320" SelectionChanged="OnSignalHistoryChanged"/>
                <ScrollViewer Grid.Row="1" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                    <Canvas x:Name="HistoryCanvas" Width="980" Height="180" Background="#FFF8FBFD"/>
                </ScrollViewer>
            </Grid>
        </GroupBox>
    </Grid>
</Window>
